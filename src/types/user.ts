export interface CookieUser {
    id: string;
    nome: string;
    sobrenome: string;
    usuario: string;
    email: string;
  };

export type Session = {
  id: string;
  nome: string;
  sobrenome: string;
  usuario: string;
  email: string;
}