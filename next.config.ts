import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['image.tmdb.org', 'njspsaopnfrhikoygxwv.supabase.co', 'images.unsplash.com'],
  },
};

export default nextConfig;
